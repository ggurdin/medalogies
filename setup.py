from setuptools import setup
import setuptools

setup(name='medalogies',
      version='0.0.1',
      description='Medical Word Embeddings',
      author='Gabby Gurdin',
      license='MIT',
      packages=setuptools.find_packages())
